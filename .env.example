# Copy this file to .env and set a real value for local development.
# This is prototype-only auth for the /admin route — never use this pattern
# in production. See README.md "Production auth warning".
VITE_ADMIN_PASSWORD=changeme
