import { ChevronLeft, ChevronRight } from 'lucide-react';
import { Container } from '@/components/ui/Container';
import { schoolProgramme } from '@/data/schoolProgramme';

export function LearningScienceExplorer({ active, onChange }: { active: number; onChange: (value: number) => void }) {
  const principles = schoolProgramme.science.principles; const item = principles[active];
  return <section className="school-section school-section--science" aria-labelledby="principles-title"><Container width="max"><div className="school-section-heading"><p className="eyebrow">Twelve established methods</p><h2 id="principles-title">Learning Principle Explorer</h2></div><div className="school-principles"><div className="school-principle-list" role="tablist" aria-label="Learning principles">{principles.map((principle, index) => <button type="button" role="tab" aria-selected={active === index} className={active === index ? 'is-active' : ''} key={principle.name} onClick={() => onChange(index)}>{index + 1}. {principle.name}</button>)}</div><article className="school-principle-panel" role="tabpanel"><p className="eyebrow">Principle {active + 1}</p><h3>{item.name}</h3><div><h4>What it means</h4><p>{item.summary}</p></div><div><h4>How Infimind applies it</h4><p>{item.application}</p></div><div><h4>In practice</h4><p>A mentor selects an age-appropriate activity, observes the learner’s response and adapts the next step.</p></div><nav aria-label="Principle navigation"><button type="button" onClick={() => onChange((active - 1 + principles.length) % principles.length)} aria-label="Previous principle"><ChevronLeft /></button><span>{active + 1} / {principles.length}</span><button type="button" onClick={() => onChange((active + 1) % principles.length)} aria-label="Next principle"><ChevronRight /></button></nav></article></div><p className="school-disclaimer">{schoolProgramme.science.disclaimer}</p></Container></section>;
}
