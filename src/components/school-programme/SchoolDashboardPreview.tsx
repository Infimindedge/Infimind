import { CalendarCheck, CheckCircle2 } from 'lucide-react';
import { Container } from '@/components/ui/Container';
import { dashboardWidgets } from '@/data/schoolProgramme';
export function SchoolDashboardPreview() { return <section className="school-section" aria-labelledby="dashboard-title"><Container width="max"><div className="school-split"><div className="school-section-heading"><p className="eyebrow">Learning made visible</p><h2 id="dashboard-title">Student Learning Dashboard</h2><p>A clear view of goals, concepts, revision and next steps—without reducing a learner to a score.</p></div><div className="school-dashboard" aria-label="Illustrative dashboard preview"><header><div><span>Illustrative dashboard preview</span><strong>Learning overview</strong></div><CalendarCheck aria-hidden="true" /></header><div>{dashboardWidgets.map(([label, state]) => <article key={label}><CheckCircle2 aria-hidden="true" /><span>{label}</span><strong>{state}</strong></article>)}</div></div></div></Container></section>; }
