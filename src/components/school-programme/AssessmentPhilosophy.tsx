import { Check } from 'lucide-react';
import { Container } from '@/components/ui/Container';
import { assessmentDimensions, schoolProgramme } from '@/data/schoolProgramme';
export function AssessmentPhilosophy() { return <section className="school-section school-section--soft" aria-labelledby="assessment-title"><Container width="max"><div className="school-assessment"><div><p className="eyebrow">Growth over grades</p><h2 id="assessment-title">Assessment That Supports Learning</h2><p>Grades matter, and they are considered alongside a fuller picture of how learning is developing.</p><ul>{schoolProgramme.assessment.map((item) => <li key={item}><Check aria-hidden="true" />{item}</li>)}</ul></div><aside className="school-assessment-orbit"><h3>Growth<br />Over Grades</h3><div>{assessmentDimensions.map((item) => <span key={item}>{item}</span>)}</div></aside></div></Container></section>; }
